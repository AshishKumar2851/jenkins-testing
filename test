pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                // DATE=`date +%d-%m-%Y`
                echo "Today's date is : $DATE"
            }
         }
    }
}
